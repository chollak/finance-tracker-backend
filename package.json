{
  "name": "finance-tracker-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "npx tsc",
    "typecheck": "npm run build",
    "serve": "node dist/index.js",
    "dev": "ts-node-dev src/index.ts",
    "dev:full": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev\" \"cd webapp && npm run dev\"",
    "dev:backend": "ts-node-dev src/index.ts",
    "dev:frontend": "cd webapp && npm run dev",
    "test": "jest",
    "test:ci": "jest --runInBand",
    "install:webapp": "cd webapp && npm install",
    "build:webapp": "cd webapp && npm run build",
    "postinstall": "npm run install:webapp && npm run build:webapp",
    "format": "prettier --write src/**/*.ts",
    "check:deps": "dependency-cruiser --validate .dependency-cruiser.js src",
    "check:circular": "madge --circular --extensions ts src",
    "analyze": "npm run check:deps && npm run check:circular",
    "verify": "npm run build && npm run test:ci && npm run build:webapp && npm run analyze",
    "supabase:setup": "npx ts-node scripts/create-tables.ts",
    "supabase:test": "npx ts-node scripts/test-supabase.ts",
    "seed:test": "npx ts-node scripts/seed-test-data.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.86.2",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^8.2.1",
    "multer": "^1.4.5-lts.2",
    "openai": "^4.89.0",
    "reflect-metadata": "^0.2.2",
    "sqlite3": "^5.1.7",
    "telegraf": "^4.12.2",
    "typeorm": "^0.3.25",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.4",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.7.6",
    "concurrently": "^9.2.0",
    "dependency-cruiser": "^17.0.1",
    "jest": "^29.7.0",
    "madge": "^8.0.0",
    "prettier": "^3.6.2",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
